pipeline {
    agent {
        docker {
            image 'maven'
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'mvn package'
                sh 'cp target/JenkinsWar.war /var/lib/tomcat9/webapps'
            }
        }
    }
}
